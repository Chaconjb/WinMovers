@{
    ViewData["Title"] = "Acceso denegado";
}

<div style="text-align:center; padding: 60px 20px;">
    <i class="fas fa-ban" style="font-size: 3.5rem; color: var(--danger); margin-bottom: 15px; display:block;"></i>
    <h2 style="color: var(--dark); margin-bottom: 8px;">Acceso denegado</h2>
    <p style="color: var(--text-muted); margin-bottom: 20px;">No tienes permisos para acceder a esta sección.</p>
    <a asp-controller="Dashboards" asp-action="Index" class="btn btn-secondary">
        <i class="fas fa-arrow-left"></i> Volver al inicio
    </a>
</div>
